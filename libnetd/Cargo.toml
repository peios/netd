[package]
name = "libnetd"
description = "netd's control wire — paths, readiness levels and the query codec."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
peios = { workspace = true }
